# financial-forecasting
Financial Forecasting model that uses linear regression, polynomial regression, decision tree, LSTM. it analyses the database and tells us which is the best model and why it is the best model out of the others. It also predicts the future of the sales or any other aspect u consider (upto 5 years)

# Financial Forecasting using Python

This project uses Python to predict financial trends using historical data.
It leverages libraries like Pandas, NumPy, and Scikit-learn for data analysis and forecasting.

## How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
# To run the script
python forecast.py
